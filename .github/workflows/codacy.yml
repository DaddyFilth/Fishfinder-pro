name: Codacy Security Scan

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    - cron: '21 13 * * 1'
  workflow_dispatch:

permissions:
  contents: read
  security-events: write
  actions: read

concurrency:
  group: codacy-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  codacy-security-scan:
    name: Codacy Security Scan
    runs-on: ubuntu-latest
    timeout-minutes: 35
    env:
      CODACY_PROJECT_TOKEN: ${{ secrets.CODACY_PROJECT_TOKEN }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Skip when Codacy token is missing
        if: env.CODACY_PROJECT_TOKEN == ''
        run: echo 'Skipping Codacy scan because CODACY_PROJECT_TOKEN is not configured.'

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: Set up Java
        uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle

      - name: Set up Android SDK
        uses: android-actions/setup-android@v4
        with:
          packages: platform-tools

      - name: Install Android SDK packages
        run: |
          yes | sdkmanager --licenses
          sdkmanager \
            "platform-tools" \
            "platforms;android-36" \
            "build-tools;36.0.0"

      - name: Install Node dependencies
        run: npm ci

      - name: Generate Android launcher icons
        run: |
          npx @capacitor/assets generate \
            --android \
            --iconBackgroundColor "#0a192f"

      - name: Build web application
        run: npm run build

      - name: Sync Capacitor Android project
        run: npx cap sync android

      - name: Grant Gradle execute permission
        run: chmod +x android/gradlew

      - name: Build Android project for Java analysis
        working-directory: android
        run: ./gradlew assembleDebug --no-daemon --stacktrace

      - name: Run Codacy Analysis CLI
        if: env.CODACY_PROJECT_TOKEN != ''
        uses: codacy/codacy-analysis-cli-action@d840f886c4bd4edc059706d09c6a1586111c540b
        with:
          project-token: ${{ env.CODACY_PROJECT_TOKEN }}
          verbose: true
          max-allowed-issues: 2147483647
